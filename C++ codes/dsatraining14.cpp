#include<iostream>
#include"LinkedListFunctions.h"
using namespace std;

int main() {
    
    // Node* first = new Node{1, nullptr};
    // inend(first, 2);
    // inend(first, 3);
    // inend(first, 4);
    // inend(first, 5);
    // inend(first, 6);
    // inend(first, 7);
    // inend(first, 8);
    // inend(first, 9);

    Node* first = nullptr;
    search(first, 10);

    return 0;
}